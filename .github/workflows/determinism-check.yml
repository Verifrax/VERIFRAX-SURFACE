name: Determinism Check

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate surface repo
        run: |
          set -euo pipefail
          scripts/validate-surface.sh
